image: openjdk:8-jdk

stages:
  - build
  - test

build project:
  stage: build
  script:
    - ./gradlew assemble

test project:
  stage: test
  script:
    - ./gradlew check -x detekt

lint project:
  stage: test
  script:
    - ./gradlew detekt
