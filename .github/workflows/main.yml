name: CI - LivingDoc
on:
  push:
  pull_request:
jobs:
  test:
    name: Test with JDK ${{ matrix.jdk }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        jdk: [8, 11, 13]
    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK ${{ matrix.jdk }}
      uses: actions/setup-java@v1
      with:
           java-version: ${{ matrix.jdk }}
    - name: Gradle Test
      run: ./gradlew check -x detekt -x spotlessCheck
    - name: Generate TestCoverage
      run:  ./gradlew codeCoverageReport
    - name: Upload coverage report
      uses: actions/upload-artifact@v1
      with:
        path: livingdoc-*/build/test-results/test/TEST-*.xml
  lint:
    name: Run linter
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up JDK 11
        uses: actions/setup-java@v1
        with:
             java-version: 11
      - name: Spotless check
        run: ./gradlew spotlessCheck
      - name: Detekt 
        run: ./gradlew detekt
  documentation:
    name: JavaDoc creation 
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up JDK 11
        uses: actions/setup-java@v1
        with:
             java-version: 11
      - name: Crate Dokka
        run: ./gradlew aggregatedDokka
      - name: Upload dokka
        uses: actions/upload-artifact@v1
        with:
          path: build/dokka
